#include<stdio.h.
void swap(int,int);
void main()
{
 int a,b;
 printf("Enter the two numbers: \n");
 scanf("%d%d",a,b);
 swap(a,b);
 }
 void swap(int a,int b)
 {
  int temp;
  temp=a;
  a=b;
  b=temp;
  printf("After swap %d%d\n",a,b);
  }
